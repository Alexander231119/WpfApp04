<UserControl x:Class="WpfApp04.Controls.KmEditTabControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WpfApp04.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="White">
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition Width="5"/>
            <ColumnDefinition/>
            <ColumnDefinition Width="5"/>
            <ColumnDefinition Width="4*"/>
        </Grid.ColumnDefinitions>

        <DataGrid x:Name="EgisKmGrid"  
              AutoGenerateColumns="False" 
              Background="#FFF0F0F0" 
              HorizontalGridLinesBrush="Gray" 
              VerticalGridLinesBrush="Gray" 
              ColumnHeaderHeight="60" 
              CanUserResizeRows="False" 
              HorizontalAlignment="Left" 
              CanUserAddRows="False" 
              CanUserDeleteRows="False" Margin="4,20,0,0" Grid.Column="2"  >
            <DataGrid.Columns>
                <DataGridTextColumn Header="Км" Binding="{Binding Path=Km}" Width="60"/>
                <DataGridTextColumn Header="Длина" Binding="{Binding Path=Length}" Width="60"/>
            </DataGrid.Columns>
        </DataGrid>

        <DataGrid x:Name="KmGrid"  
              AutoGenerateColumns="False" 
              Background="#FFF0F0F0" 
              HorizontalGridLinesBrush="Gray" 
              VerticalGridLinesBrush="Gray" ColumnHeaderHeight="60" CanUserResizeRows="False" HorizontalAlignment="Left" CanUserAddRows="False" CanUserDeleteRows="False" SelectionChanged="KmGrid_SelectionChanged" Margin="0,20,0,0"  >
            <DataGrid.Columns>
                <DataGridTextColumn Header="Км" Binding="{Binding Km}" Width="60"/>
                <DataGridTextColumn Header="Длина" Binding="{Binding Length}" Width="60"/>
            </DataGrid.Columns>
        </DataGrid>

        <GridSplitter Grid.Column="3"  Grid.Row="0" ShowsPreview="False" Width="5"
                  HorizontalAlignment="Center" />
        <TextBlock Background="WhiteSmoke" x:Name="KmTextBlock1" Grid.Column="4" HorizontalAlignment="Left" Margin="10,33,0,0" TextWrapping="Wrap" Text=" " VerticalAlignment="Top" Width="783" Height="63" />
        <TextBlock x:Name="KmTextBlock2" Grid.Column="4" HorizontalAlignment="Left" Margin="10,10,0,0" TextWrapping="Wrap" Text="Длина:" VerticalAlignment="Top" Width="43" Height="17" />
        <TextBox x:Name="DbKmTextBox" Grid.Column="4" HorizontalAlignment="Left" Margin="58,10,0,0" TextWrapping="Wrap" Text="" VerticalAlignment="Top" Width="60"/>
        <Button x:Name="DbKmSetLengthButton" Grid.Column="4" Content="Задать длину" HorizontalAlignment="Left" Margin="123,10,0,0" VerticalAlignment="Top" Height="18" Width="108" Click="DbKmSetLengthButton_Click"/>
        <Button x:Name="DbKmSegmentGroupSetLengthButton" Grid.Column="4" Content="Задать общую длину" HorizontalAlignment="Left" Margin="256,10,0,0" VerticalAlignment="Top" Height="18" Width="146" Click="DbKmSegmentGroupSetLengthButton_Click"/>
    </Grid>
</UserControl>

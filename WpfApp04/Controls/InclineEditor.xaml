<UserControl x:Class="WpfApp04.Controls.InclineEditor"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WpfApp04.Controls"
             SizeChanged="OnSizeChanged"
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="3" Margin="3" VerticalAlignment="Center" x:Name="SegmentTextBlock"/>
        <ScrollViewer Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="3" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Disabled" x:Name="ScrollViewer" ScrollChanged="ScrollViewer_OnScrollChanged">
            <local:DrawingCanvas x:Name="DrawingCanvas" ClipToBounds="True" Background="Transparent"/>
        </ScrollViewer>
        <Slider Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="3" Margin="3" x:Name="ScaleSlider" ValueChanged="ScaleSliderValueChanged"/>
        <GroupBox Grid.Row="4" Grid.Column="0" Grid.ColumnSpan="2" Header="Курсор">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <TextBlock Grid.Row="0" Grid.Column="0" Text="Уклон:" VerticalAlignment="Center" Margin="3"/>
                <TextBlock Grid.Row="1" Grid.Column="0" Text="Железнодорожная координата:" VerticalAlignment="Center" Margin="3"/>
                <TextBlock Grid.Row="2" Grid.Column="0" Text="Координата на ребре:" VerticalAlignment="Center" Margin="3"/>
                <TextBlock Grid.Row="0" Grid.Column="1" x:Name="PointInclineTextBlock" VerticalAlignment="Center" Margin="3"/>
                <TextBlock Grid.Row="1" Grid.Column="1" x:Name="PointRailWayTextBlock" VerticalAlignment="Center" Margin="3"/>
                <TextBlock Grid.Row="2" Grid.Column="1" x:Name="PointCoordinateTextBlock" VerticalAlignment="Center" Margin="3"/>
                
            </Grid>
        </GroupBox>
    </Grid>
</UserControl>

<UserControl x:Class="WpfApp04.Controls.ImportOptionsControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WpfApp04.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="250" d:DesignWidth="160">
    <Grid Background="Transparent">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Левая колонка -->
        <StackPanel Grid.Column="0" Margin="4">
            <CheckBox x:Name="ImportTrafficLightsCheckBox" Content="Светофоры" Margin="0,3"/>
            <CheckBox x:Name="ImportPlatformsCheckBox" Content="Платформы" Margin="0,3"/>
            <CheckBox x:Name="ImportUkspsCheckBox" Content="Укспс" Margin="0,3"/>
            <CheckBox x:Name="ImportKtsmCheckBox" Content="КТСМ" Margin="0,3"/>
            <CheckBox x:Name="ImportSignsCheckBox" Content="Сигнальный знак" Margin="0,3"/>
            <CheckBox x:Name="ImportCrossingsCheckBox" Content="Переезд" Margin="0,3"/>
            <CheckBox x:Name="ImportNeutralSectionsCheckBox" Content="Нейтральная вставка" Margin="0,3"/>
            <CheckBox x:Name="ImportCurrentChangeCheckBox" Content="Точка смены рода тока" Margin="0,3"/>
        
            <CheckBox x:Name="ImportInclinesCheckBox" Content="Уклоны" Margin="0,3"/>
            <CheckBox x:Name="ImportSpeedCheckBox" Content="Ограничения скорости" Margin="0,3"/>
            <CheckBox x:Name="ImportStationBordersCheckBox" Content="Границы станций" Margin="0,3"/>
        </StackPanel>
    </Grid>
</UserControl>
